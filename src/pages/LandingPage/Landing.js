import React from "react";
import { Wrapper, Menu, Card } from "./styles";

const Landing = ({ menu }) => {
  return (
    <Wrapper>
      Landing Page
    </Wrapper>
  );
};

export default Landing;
